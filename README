1.
…or create a new repository on the command line
echo "# TourPackets" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/saalexworld/TourPackets.git
git push -u origin master

2.
…or push an existing repository from the command line
git remote add origin https://github.com/saalexworld/TourPackets.git
git branch -M master
git push -u origin master

3.
При необходимости сменить гит репозиторий:
git remote set-url origin https://github.com/saalexworld/TourPackets.git
Проверить смену репозитория:
cat .git/config

4.
Info
Запуск селери:
celery -A Tours worker --loglevel=INFO
